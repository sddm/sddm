language: cpp

compiler:
 - gcc
 - clang

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq cmake qt4-dev-tools libpam0g-dev libX11-dev

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make
